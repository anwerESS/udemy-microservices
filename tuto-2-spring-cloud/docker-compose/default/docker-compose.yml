services:

  # Loki Read Component - Handles log queries
  # Exposes port 3101 for external query requests
  # Connects to MinIO for log storage retrieval
  read:
    image: grafana/loki:2.8.3
    command: "-config.file=/etc/loki/config.yaml -target=read"  # Runs in read-only mode
    ports:
      - 3101:3100  # External query endpoint
      - 7946       # Cluster memberlist port
      - 9095       # Metrics endpoint
    volumes:
      - ../observability/loki/loki-config.yaml:/etc/loki/config.yaml  # Mount config file
    depends_on:
      - minio  # Requires storage backend
    healthcheck:
      test: [ "CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3100/ready || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks: &loki-dns
      anweress:
        aliases:
          - loki  # DNS alias for service discovery

  # Loki Write Component - Handles log ingestion
  # Exposes port 3102 for log forwarding
  # Stores logs in MinIO object storage
  write:
    image: grafana/loki:2.8.3
    command: "-config.file=/etc/loki/config.yaml -target=write"  # Runs in write-only mode
    ports:
      - 3102:3100  # External write endpoint
      - 7946       # Cluster memberlist port
      - 9095       # Metrics endpoint
    volumes:
      - ../observability/loki/loki-config.yaml:/etc/loki/config.yaml  # Mount config file
    healthcheck:
      test: [ "CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3100/ready || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5
    depends_on:
      - minio  # Requires storage backend
    networks:
      <<: *loki-dns  # Inherits network config from read service

  # Promtail - Log collection agent
  # Scrapes Docker container logs and forwards to Loki
  # Requires access to Docker daemon socket
  promtail:
    image: grafana/promtail:2.8.3
    volumes:
      - ../observability/promtail/promtail-local-config.yaml:/etc/promtail/config.yaml:ro  # Read-only config
      - /var/run/docker.sock:/var/run/docker.sock  # Access to Docker API
    command: -config.file=/etc/promtail/config.yaml
    depends_on:
      - gateway  # Requires gateway to be running
    extends:
      file: common-config.yml
      service: network-deploy-service
  # MinIO - S3-compatible object storage
  # Stores Loki log chunks and indexes
  # Pre-creates required buckets on startup
  #      mkdir -p /data/loki-data && \  # Main log storage
  #      mkdir -p /data/loki-ruler && \ # Alert rules storage
  #      minio server /data  # Start server
  minio:
    image: minio/minio
    entrypoint:
      - sh
      - -c
      - |
        mkdir -p /data/loki-data && \
        mkdir -p /data/loki-ruler && \
        minio server /data
    environment:
      - MINIO_ROOT_USER=loki  # Admin username
      - MINIO_ROOT_PASSWORD=supersecret  # Admin password
    volumes:
      - ./.data/minio:/data  # Persistent storage
    extends:
      file: common-config.yml
      service: network-deploy-service

  # Grafana - Visualization dashboard
  # Auto-configures Loki datasource on startup
  # Exposes web UI on port 3000
  grafana:
    image: grafana/grafana:11.4.0
    environment:
      - GF_PATHS_PROVISIONING=/etc/grafana/provisioning
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
    depends_on:
        - gateway
    entrypoint:
      - sh
      - -euc
      - |
        mkdir -p /etc/grafana/provisioning/datasources
        cat <<EOF > /etc/grafana/provisioning/datasources/ds.yaml
        apiVersion: 1
        datasources:
          - name: Loki
            type: loki
            access: proxy
            url: http://gateway:3100
            jsonData:
              httpHeaderName1: "X-Scope-OrgID"
            secureJsonData:
              httpHeaderValue1: "tenant1"
        EOF
        /run.sh
    ports:
      - "3000:3000"  # Web interface
    healthcheck:
      test: [ "CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3000/api/health || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5
    extends:
      file: common-config.yml
      service: network-deploy-service

  # NGINX Gateway - Unified access point
  # Routes requests to appropriate Loki components
  # Provides load balancing and single endpoint
  gateway:
    image: nginx:1.27.3
    depends_on:
      - read
      - write
    entrypoint:
      - sh
      - -euc
      - |
        cat <<EOF > /etc/nginx/nginx.conf
        user  nginx;
        worker_processes  5;

        events {
          worker_connections   1000;
        }

        http {
          resolver 127.0.0.11;

          server {
            listen             3100;

            location = / {
              return 200 'OK';
              auth_basic off;
            }

            location = /api/prom/push {
              proxy_pass       http://write:3100\$$request_uri;
            }

            location = /api/prom/tail {
              proxy_pass       http://read:3100\$$request_uri;
              proxy_set_header Upgrade \$$http_upgrade;
              proxy_set_header Connection "upgrade";
            }

            location ~ /api/prom/.* {
              proxy_pass       http://read:3100\$$request_uri;
            }

            location = /loki/api/v1/push {
              proxy_pass       http://write:3100\$$request_uri;
            }

            location = /loki/api/v1/tail {
              proxy_pass       http://read:3100\$$request_uri;
              proxy_set_header Upgrade \$$http_upgrade;
              proxy_set_header Connection "upgrade";
            }

            location ~ /loki/api/.* {
              proxy_pass       http://read:3100\$$request_uri;
            }
          }
        }
        EOF
        /docker-entrypoint.sh nginx -g "daemon off;"
    ports:
      - "3100:3100"
    healthcheck:
      test: ["CMD", "service", "nginx", "status"]
      interval: 10s
      timeout: 5s
      retries: 5
    extends:
      file: common-config.yml
      service: network-deploy-service

#  redis:
#    image: redis
#    ports:
#      - "6379:6379"
#    healthcheck:
#      test: [ "CMD-SHELL", "redis-cli ping | grep PONG" ]
#      timeout: 10s
#      retries: 10
#    extends:
#      file: common-config.yml
#      service: network-deploy-service
#  rabbit:
#    image: rabbitmq:3.13-management  # Uses the RabbitMQ image with the management plugin enabled.
#    hostname: rabbitmq  # Sets the hostname of the container to "rabbitmq".
#    ports:
#      - "5672:5672"  # Exposes RabbitMQ's default AMQP port (5672) for messaging.
#      - "15672:15672"  # Exposes RabbitMQ's management UI port (15672) for monitoring.
#    healthcheck:
#      test: rabbitmq-diagnostics check_port_connectivity  # Checks if RabbitMQ is reachable.
#      interval: 10s  # Runs the health check every 10 seconds.
#      timeout: 5s  # Sets a 5-second timeout for the health check.
#      retries: 10  # Retries the health check up to 10 times before marking the service as unhealthy.
#      start_period: 5s  # Waits 5 seconds before starting health checks (to allow RabbitMQ to initialize).
#    extends:
#      file: common-config.yml  # Extends configuration from the `common-config.yml` file.
#      service: network-deploy-service  # Uses the `network-deploy-service` configuration.

  accountsdb:
    container_name: accountsdb
    ports:
      - 3306:3306 # noneed to expose the port (but it our case is needed because we use a client to connect to the db)
    environment:
      MYSQL_DATABASE: accountsdb
    extends:
      file: common-config.yml
      service: microservice-db-config

  loansdb:
    container_name: loansdb
    ports:
      - 3307:3306
    environment:
      MYSQL_DATABASE: loansdb
    extends:
      file: common-config.yml
      service: microservice-db-config

  cardsdb:
    container_name: cardsdb
    ports:
      - 3308:3306
    environment:
      MYSQL_DATABASE: cardsdb
    extends:
      file: common-config.yml
      service: microservice-db-config

  eurekaserver:
    image: "anweress/eurekaserver:s11"
    container_name: eurekaserver-ms
    ports:
      - "8070:8070"
    depends_on:
      configserver:
        condition: service_healthy
    healthcheck:
      test: "curl --fail --silent localhost:8070/actuator/health/readiness | grep UP || exit 1"
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    extends:
      file: common-config.yml
      service: microservice-configserver-config
    environment:
      SPRING_APPLICATION_NAME: "eurekaserver"

  configserver:
    image: "anweress/configserver:s11"  # Uses a custom Docker image for the Config Server.
    container_name: configserver-ms  # Sets the container name to "configserver-ms".
    ports:
      - "8071:8071"  # Exposes port 8071 for the Config Server.
#    depends_on:
#      rabbit:
#        condition: service_healthy  # Ensures the RabbitMQ service is healthy before starting.
    healthcheck:
      test: "curl --fail --silent localhost:8071/actuator/health/readiness | grep UP || exit 1"  # Checks if the Config Server is ready.
      interval: 10s  # Runs the health check every 10 seconds.
      timeout: 5s  # Sets a 5-second timeout for the health check.
      retries: 10  # Retries the health check up to 10 times before marking the service as unhealthy.
      start_period: 10s  # Waits 10 seconds before starting health checks (to allow the Config Server to initialize).
    extends:
      file: common-config.yml  # Extends configuration from the `common-config.yml` file.
      service: microservice-base-config  # Uses the `microservice-base-config` configuration.

  accounts:
    image: "anweress/accounts:s11"  # Uses a custom Docker image for the Accounts microservice.
    container_name: accounts-ms  # Sets the container name to "accounts-ms".
    ports:
      - "8080:8080"  # Exposes port 8080 for the Accounts microservice.
    depends_on:
      configserver:
        condition: service_healthy  # Ensures the Config Server is healthy before starting.
      eurekaserver:
          condition: service_healthy
    healthcheck:
      test: "curl --fail --silent localhost:8080/actuator/health/readiness | grep UP || exit 1"
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    environment:
      SPRING_APPLICATION_NAME: "accounts"  # Sets the Spring application name to "accounts".
      SPRING_DATASOURCE_URL: "jdbc:mysql://accountsdb:3306/accountsdb"
    extends:
      file: common-config.yml  # Extends configuration from the `common-config.yml` file.
      service: microservice-eureka-config  # Uses the `microservice-eureka-config` configuration.

  loans:
    image: "anweress/loans:s11"  # Uses a custom Docker image for the Loans microservice.
    container_name: loans-ms  # Sets the container name to "loans-ms".
    ports:
      - "8090:8090"  # Exposes port 8090 for the Loans microservice.
    depends_on:
      eurekaserver:
        condition: service_healthy
      configserver:
        condition: service_healthy  # Ensures the Config Server is healthy before starting.
    healthcheck:
      test: "curl --fail --silent localhost:8090/actuator/health/readiness | grep UP || exit 1"
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    environment:
      SPRING_APPLICATION_NAME: "loans"  # Sets the Spring application name to "loans".
      SPRING_DATASOURCE_URL: "jdbc:mysql://loansdb:3306/loansdb" # must be 3306 and not 3307 because database start inside the docker network
    extends:
      file: common-config.yml  # Extends configuration from the `common-config.yml` file.
      service: microservice-eureka-config  # Uses the `microservice-eureka-config` configuration.

  cards:
    image: "anweress/cards:s11"  # Uses a custom Docker image for the Cards microservice.
    container_name: cards-ms  # Sets the container name to "cards-ms".
    ports:
      - "9000:9000"  # Exposes port 9000 for the Cards microservice.
    depends_on:
      eurekaserver:
        condition: service_healthy
      configserver:
        condition: service_healthy  # Ensures the Config Server is healthy before starting.
    healthcheck:
      test: "curl --fail --silent localhost:9000/actuator/health/readiness | grep UP || exit 1"
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    environment:
      SPRING_APPLICATION_NAME: "cards"  # Sets the Spring application name to "cards".
      SPRING_DATASOURCE_URL: "jdbc:mysql://cardsdb:3306/cardsdb" #  must be 3306 and not 3307 because database start inside the docker network
    extends:
      file: common-config.yml  # Extends configuration from the `common-config.yml` file.
      service: microservice-eureka-config  # Uses the `microservice-eureka-config` configuration.

  gatewayserver:
    image: "anweress/gatewayserver:s11"
    container_name: gatewayserver-ms
    ports:
      - "8072:8072"
    depends_on:
      accounts:
        condition: service_healthy
      loans:
        condition: service_healthy
      cards:
        condition: service_healthy
#      redis:
#        condition: service_healthy
    environment:
      SPRING_APPLICATION_NAME: "gatewayserver"
#      SPRING_DATA_REDIS_CONNECT-TIMEOUT: 2s
#      SPRING_DATA_REDIS_HOST: redis
#      SPRING_DATA_REDIS_PORT: 6379
#      SPRING_DATA_REDIS_TIMEOUT: 1s
    extends:
      file: common-config.yml
      service: microservice-eureka-config

networks:
  anweress:
    driver: "bridge"  # Creates a bridge network named "anweress" for communication between containers.